apiVersion: v1
kind: Service
metadata:
  name: frontend-seminario-svc
  namespace: microservices
spec:
  selector:
    app: frontend-seminario
  ports:
    - protocol: TCP
      port: 3010
      targetPort: 80
  type: ClusterIP